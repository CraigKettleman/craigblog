# 2026-08-06

## 深度研究报告（craigblog 全仓库）
- 任务：对 `/Users/craig/Documents/craigblog` 逐文件深度分析，产出研究报告。
- 产出：`/Users/craig/craigblog-architecture-report.html`（758 行，自包含 HTML，含 TOC/架构/逐模块/依赖总结）。
- 关键发现：
  - **双部署冲突**：README/AGENTS.md/copilot-instructions/wrangler.jsonc 描述 Cloudflare Workers，但 `svelte.config.js` 用 `adapter-node`、`deploy/deploy.sh`+`nginx.conf`+`hhy-homes.service` 部署到 VPS(124.222.217.211)。`/api/social` 与 `/feed` 用 `platform.caches/KV` 但已用可选链降级。
  - 内容仓库为骨架态：`content/posts` 与 `content/life-posts` 未提交文章，仅 `content/categories/*.yml` 存在；Velite `prepare()` 会在 build 期校验分类。
  - 评论/会员用本地 JSON 文件存储（users.json/comments.json），scrypt+HMAC+限流+蜜罐；会话令牌存 localStorage（客户端会话）。
  - 视觉主题为「打印机」：PrinterShell + Printed* 原语 + PrinterSnail/RotaryDial/LightSwitch/Stickers 装饰件。
- 注意：本环境 `Read` 工具对源码返回空，改用 `Bash cat` 读取文件内容完成分析。
